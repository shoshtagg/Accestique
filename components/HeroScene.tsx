'use client';

export default function HeroScene() {
    return (
        <div className="absolute inset-0 z-0 opacity-60">
            <div className="w-full h-full bg-gradient-to-br from-cyan-900/20 via-blue-900/20 to-purple-900/20 animate-pulse"></div>
        </div>
    );
}
